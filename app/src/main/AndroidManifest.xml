<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xknove">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XKnove">
        <activity android:name=".newlogin"></activity>
        <activity android:name=".more" />
        <activity
            android:name=".betaa"
            android:label="@string/title_activity_betaa"
            android:theme="@style/Theme.XKnove.NoActionBar" />
        <activity android:name=".forum" />
        <activity android:name=".userprofile" />
        <activity android:name=".people" />
        <activity android:name=".aitask" />
        <activity android:name=".assistant" />
        <activity android:name=".play" />
        <activity android:name=".game1" />
        <activity android:name=".time" />
        <activity android:name=".MainActivity" />
        <activity android:name=".reg" />
        <activity android:name=".home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>